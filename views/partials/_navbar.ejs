
  <div class="hero-head">
    <% if(locals.isHomepage) { %> //this is displaying the navbar as transparent on the home page and not on the others.
    <nav class="navbar home-nav">
    <% } else { %>
    <nav class="navbar">
    <% } %>
      <div class="container">
      <div class="navbar-brand">
        <a class="navbar-item secondary-font is-size-6" href="/">
          <img src="https://image.flaticon.com/icons/svg/35/35185.svg" alt="Art">
          &nbsp;
          Tag Mate Home
        </a>
         <div class="navbar-burger burger" data-target="#mobile-menu" onclick="document.querySelector('.navbar-menu').classList.toggle('is-active');">
          <span></span>
          <span></span>
          <span></span>
        </div>
      </div>
      <div id="mobile-menu" class="navbar-menu">
        <div class="navbar-end">
          <a class="navbar-item secondary-font is-size-6" href="/arts">View Art</a>
          <% if(locals.isAuthenticated) { %>
            <a class="navbar-item secondary-font is-size-6" href="/arts/new">Add Art</a>
            <a class="navbar-item secondary-font is-size-6" href="/logout">Logout</a>
          <% } else { %>
            <a class="navbar-item secondary-font is-size-6" href="/register">Register</a>
            <a class="navbar-item secondary-font is-size-6" href="/login">Login</a>
          <% } %>
        </div>
      </div>
    </nav>
  </div>
</div>
